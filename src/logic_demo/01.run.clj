;; RUN
;; main entry point to a core.logic program

  (run n [q] ...goals...)

  (run* [q] ...goals...)












(run 1 [q])
;; => (_.0)












(run 5 [q])
;; => (_.0)















(run* [q])
;; => (_.0)















